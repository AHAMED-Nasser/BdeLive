document.addEventListener("DOMContentLoaded", function () { let e = document.getElementById("year-select"), t = document.getElementById("group-select"); if (!e || !t) return; let n = window.scheduleGroupsData || {}; function o() { let n = e.value, o = t.value; if (n) { if ("function" == typeof window.scheduleLoadView && o) { let l = new URLSearchParams(window.location.search), i = l.get("view") || "week"; window.scheduleLoadView(i, { year: n, group: o, _scrollToEdt: !0 }) } else { let a = new URLSearchParams; a.set("page", "schedule"), a.set("year", n), o && a.set("group", o); let r = "index.php?" + a.toString(); o && (r += "#scroll-edt"), window.location.href = r } } } e.addEventListener("change", function () { let e = this.value; t.innerHTML = '<option value="">-- S\xe9lectionner un groupe --</option>', e && n[e] && Object.entries(n[e]).forEach(function ([e, n]) { let o = document.createElement("option"); o.value = e, o.textContent = n, t.appendChild(o) }), o() }), t.addEventListener("change", o) });
