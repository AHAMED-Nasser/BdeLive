!function () { "use strict"; let e = document.querySelector('meta[name="csrf-token"]')?.content; if (!e) { console.warn("CSRF Handler: Token not found in meta tag. CSRF protection may not work correctly."); return } let n = window.fetch; window.fetch = function (t, r = {}) { if (r.headers || (r.headers = {}), r.headers instanceof Headers) { let o = {}; r.headers.forEach((e, n) => { o[n] = e }), r.headers = o } return r.headers["X-CSRF-Token"] || r.headers["x-csrf-token"] || (r.headers["X-CSRF-Token"] = e), n(t, r) }, window.getCsrfToken = function () { return e }, console.log("CSRF Handler: Initialized successfully") }();
