let slideIndexes = {}; const AUTO_PLAY_INTERVAL_MS = 5e3, HOME_CAROUSEL_ID = "carousel-future-event"; let autoPlayTimers = {}; function moveSlide(e, t) { slideIndexes[t] || (slideIndexes[t] = 1), showSlide(slideIndexes[t] += e, t), resetAutoPlay(t) } function currentSlide(e, t) { showSlide(slideIndexes[t] = e + 1, t), resetAutoPlay(t) } function showSlide(e, t) { let l = document.getElementById(t), s = l.querySelectorAll(".carousel-item"), i = l.querySelectorAll(".dot"); slideIndexes[t] || (slideIndexes[t] = 1), e > s.length && (slideIndexes[t] = 1), e < 1 && (slideIndexes[t] = s.length), s.forEach(e => e.classList.remove("active")), i.forEach(e => e.classList.remove("active")), s[slideIndexes[t] - 1].classList.add("active"), i[slideIndexes[t] - 1].classList.add("active") } function stopAutoPlay(e) { autoPlayTimers[e] && (clearInterval(autoPlayTimers[e]), autoPlayTimers[e] = null) } function startAutoPlay(e) { let t = document.getElementById(e); if (!t) return; let l = t.querySelectorAll(".carousel-item"); l.length <= 1 || (stopAutoPlay(e), autoPlayTimers[e] = setInterval(() => { moveSlide(1, e) }, 5e3)) } function resetAutoPlay(e) { e === HOME_CAROUSEL_ID && (stopAutoPlay(e), startAutoPlay(e)) } function initCarousels() { let e = document.querySelectorAll(".carousel"); e.forEach(e => { let t = e.id; t && (slideIndexes[t] = 1, showSlide(1, t), t === HOME_CAROUSEL_ID && startAutoPlay(t)) }) } document.addEventListener("DOMContentLoaded", initCarousels), document.addEventListener("visibilitychange", () => { if (document.hidden) stopAutoPlay(HOME_CAROUSEL_ID); else { let e = document.getElementById(HOME_CAROUSEL_ID); e && e.querySelectorAll(".carousel-item").length > 1 && startAutoPlay(HOME_CAROUSEL_ID) } });
